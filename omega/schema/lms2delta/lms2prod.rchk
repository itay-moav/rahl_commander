#test coments

#test comment after an empty line
#test multiple lines rule with tabs/spaces

all:
    ignore_tables[user_roles,organization_user_enrollment,rbac_user,organization_education_grouptobedeleted,organization_education_group_usertobedeleted,rbac_user_grouptobedeleted] 
    same[type,ignore_fields(date_stored|delta_type|delta_note|delta_rbac_user_id)]

#test single line rule
course_enrollment:
    same[type,ignore_fields(date_stored|delta_type|delta_note|delta_rbac_user_id)]
