# Rule makes sure that if dhara_delta_2.first_file_members exists, then it will have
# the same field names and field types as dhara.first_file_members.
# But, it will ignore the field (if exists) dhara_delta_2.first_file_members.modified_by

first_file_members:same_if_exists[type,ignore_fields(modified_by)]


